{
  "name": "ractive-brunch",
  "version": "1.0.0",
  "description": "Adds Ractive.js support to brunch.",
  "author": "Graeme Coupar (@obmarg)",
  "homepage": "https://github.com/obmarg/ractive-brunch",
  "repository": {
    "type": "git",
    "url": "git@github.com:obmarg/ractive-brunch.git"
  },
  "main": "./lib/index",
  "scripts": {
    "prepublish": "rm -rf lib && coffee --bare --output lib/ src/",
    "test": "node_modules/.bin/mocha --require test/common.js"
  },
  "dependencies": {
    "ractive": "0.3.5",
    "umd-wrapper": "~0.1.0"
  },
  "devDependencies": {
    "mocha": "1.11.0",
    "chai": "1.7.0"
  }
}
